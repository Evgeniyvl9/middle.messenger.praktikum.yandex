<div class="input-wrap">
    {{#if label}}
        <label for="{{id}}">{{ label }}</label>
    {{/if}}
    <input type="{{ type }}" name="{{ name }}" id="{{ id }}" value="{{value}}" >
    <div class="error_mes">{{errorText}}</div>
</div>
